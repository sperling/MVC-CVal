* Make sure js works with radiobutton, select.
* Set properties in AssemblyInfo.cs
* Remote are showing default message on first request(when validation fails and not returning validation message).
  Should show the specified message...